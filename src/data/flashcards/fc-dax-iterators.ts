// Sprint 4 — DAX iterators + RLS-DAX patterns deck (12 cards).
// Front = signature/pattern; back = MECHANISM + WHEN TO USE.

import type { Flashcard } from '../../lib/schema';

export const daxIteratorsDeck: Flashcard[] = [
  // Iterators
  { id: 'fc-dxi-001', deck: 'dax-advanced', difficulty: 2,
    front: 'SUMX vs SUM — when do you need SUMX?',
    back: 'When the expression iterates per row (e.g., `Quantity * UnitPrice`). SUM accepts only a single column. SUMX(table, expr) iterates and sums. Always prefer simple SUM when the inner is a single column — it pushes to storage engine.',
    tags: ['sumx', 'aggregators'],
    sourceAnchor: { category: 'dax-functions', note: 'Iterator vs aggregator basics' } },
  { id: 'fc-dxi-002', deck: 'dax-advanced', difficulty: 3,
    front: 'Context transition — what triggers it inside an iterator?',
    back: 'Calling a MEASURE inside an iterator triggers context transition: the iterator\'s row context is converted to filter context for the measure. Same effect as wrapping the measure body in CALCULATE.',
    tags: ['context-transition', 'iterators', 'measures'],
    sourceAnchor: { category: 'dax-functions', note: 'Context transition mechanism' } },
  { id: 'fc-dxi-003', deck: 'dax-advanced', difficulty: 4,
    front: 'AVERAGEX(Sales, Sales[Amount]) vs AVERAGE(Sales[Amount]) — same result?',
    back: 'Same RESULT, very different PERFORMANCE. AVERAGEX iterates in formula engine (slow on 100M+ rows); AVERAGE pushes to VertiPaq (storage engine, milliseconds). Use simple aggregators when the expression is a single column.',
    tags: ['averagex', 'performance', 'storage-engine'],
    sourceAnchor: { category: 'dax-performance', note: 'Iterator vs simple aggregator perf' } },
  { id: 'fc-dxi-004', deck: 'dax-advanced', difficulty: 4,
    front: 'RANKX in a matrix visual returns 1 for every row — why?',
    back: 'Matrix visual filters dimension to a single row per cell. RANKX over a 1-row table = rank 1 always. Wrap with ALL: `RANKX(ALL(Customer), [Total Sales], , DESC)`.',
    tags: ['rankx', 'all', 'matrix-visual', 'exam-trap'],
    sourceAnchor: { category: 'dax-functions', note: 'RANKX + ALL pattern' } },
  { id: 'fc-dxi-005', deck: 'dax-advanced', difficulty: 4,
    front: 'TOPN with ties at the boundary — how many rows?',
    back: 'TOPN returns ALL tied rows at the boundary. If 2 customers tie for 5th place, TOPN(5,...) returns 6 rows. Add a tiebreaker `OrderBy_Expression2` to force exactly N.',
    tags: ['topn', 'ties', 'exam-trap'],
    sourceAnchor: { category: 'dax-functions', note: 'TOPN tie behavior' } },
  { id: 'fc-dxi-006', deck: 'dax-advanced', difficulty: 4,
    front: 'CALCULATE filter modifier evaluation order',
    back: 'REMOVEFILTERS / KEEPFILTERS / overrides evaluate FIRST, then the new filter args apply. So `CALCULATE(m, REMOVEFILTERS(Date), Date[Year]=2026)` clears Date THEN sets Year=2026 — the visual context is overridden.',
    tags: ['calculate', 'filter-modifier', 'evaluation-order'],
    sourceAnchor: { category: 'dax-functions', note: 'CALCULATE filter arg order' } },
  { id: 'fc-dxi-007', deck: 'dax-advanced', difficulty: 5,
    front: 'OFFSET / INDEX / WINDOW — what do they need to work?',
    back: 'A relative-position computation needs ORDERBY (sort key) and optionally PARTITIONBY (scope). `OFFSET(-1, ORDERBY(Date), PARTITIONBY(Customer))` = previous-row value within the same customer.',
    tags: ['offset', 'index', 'window', 'partitionby'],
    sourceAnchor: { category: 'dax-functions', note: 'Window functions' } },
  { id: 'fc-dxi-008', deck: 'dax-advanced', difficulty: 4,
    front: 'VAR — what does it actually do?',
    back: 'Captures a value ONCE at declaration row context, then returns it every time the name is referenced. Improves performance (no recomputation) AND readability. Does NOT perform context transition.',
    tags: ['var', 'caching', 'readability'],
    sourceAnchor: { category: 'dax-functions', note: 'Variable scoping + caching' } },

  // RLS DAX patterns
  { id: 'fc-rdx-001', deck: 'security-governance', difficulty: 3,
    front: 'Static RLS filter syntax',
    back: '`[Column] = "Value"`. Boolean predicate. Cheapest pattern; no context transition; compiles to CALCULATETABLE.',
    tags: ['rls', 'static', 'syntax'],
    sourceAnchor: { category: 'row-level-security', note: 'Static filter pattern' } },
  { id: 'fc-rdx-002', deck: 'security-governance', difficulty: 3,
    front: 'Dynamic RLS via UPN',
    back: '`[UserEmail] = USERPRINCIPALNAME()`. Dynamic — every user sees rows matching their UPN. Most common dynamic pattern.',
    tags: ['rls', 'dynamic', 'userprincipalname'],
    sourceAnchor: { category: 'row-level-security', note: 'Dynamic UPN filter' } },
  { id: 'fc-rdx-003', deck: 'security-governance', difficulty: 4,
    front: 'Dynamic RLS via LOOKUPVALUE — security gotcha?',
    back: '`[Key] = LOOKUPVALUE(Users[Key], Users[Email], USERPRINCIPALNAME())` — if user is missing from Users, LOOKUPVALUE returns BLANK, and `[Key] = BLANK` MATCHES rows with NULL Key. ADD GUARD: `&& NOT(ISBLANK(_lookup))`.',
    tags: ['rls', 'lookupvalue', 'blank-trap', 'security-gap'],
    sourceAnchor: { category: 'row-level-security', note: 'LOOKUPVALUE BLANK guard' } },
  { id: 'fc-rdx-004', deck: 'security-governance', difficulty: 4,
    front: 'RLS cross-filter direction surprise',
    back: 'Single-direction relationship: dim filters fact (typical). If the role expects dim-filter to reach the fact through a non-typical relationship, set CrossFilterDirection=Both on the role definition (or in the relationship). Symptom: fact rows ignore the dim filter.',
    tags: ['rls', 'cross-filter', 'troubleshooting'],
    sourceAnchor: { category: 'row-level-security', note: 'Cross-filter direction for RLS' } }
];
